{
  "name": "model-exporter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-deps": "node node_modules/puppeteer/install.js",
    "start-server": "docker-compose up -d",
    "stop-server": "docker-compose stop",
    "export": "node export-diagrams.js http://localhost:8080/workspace/diagrams png $MODEL"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^20.3.0"
  }
}
